// 1. Implement enqueue() in JS
// Write a function that inserts a number to the tail with two arguments

const ages = [7, 5, 1, 3, 4]

function getEnqueue(array, num){
    return array[array.length] = num
}
const num1 = 9
getEnqueue(ages, num1)
console.log(ages)